"use client";

// This is a placeholder page to handle LIFF redirects.
// The main logic is in the layout and the [bookingId] page.
export default function ReviewRedirectPage() {
  return (
    <div className="p-4 text-center">
      <p>กำลังโหลดข้อมูลรีวิว...</p>
    </div>
  );
}
